
// Import the refactored hook and re-export it
import { useContentPlanner } from './content-planner/useContentPlanner';

export { useContentPlanner };

// Default export for backward compatibility
export default useContentPlanner;
